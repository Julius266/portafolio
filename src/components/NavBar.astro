<nav class="border-b border-gray-200 dark:border-gray-700 bg-white dark:bg-[#111921] sticky top-0 z-50 transition-colors">
    <div class="px-4 sm:px-6 lg:px-8 py-3 max-w-6xl w-full mx-auto">
        <!-- Header con logo y botones -->
        <div class="flex items-center justify-between">
            <!-- Logo -->
            <a href="#home" class="text-gray-900 dark:text-gray-100 text-lg font-bold leading-tight tracking-[-0.015em] hover:text-blue-600 dark:hover:text-blue-400 transition-colors">Julio Vinces</a>

            <!-- Desktop Navigation -->
            <div class="hidden md:flex items-center gap-6">
                <a href="#home" class="text-gray-700 dark:text-gray-300 text-sm font-medium leading-normal hover:text-blue-600 dark:hover:text-blue-400 transition-colors">Inicio</a>
                <a href="#about" class="text-gray-700 dark:text-gray-300 text-sm font-medium leading-normal hover:text-blue-600 dark:hover:text-blue-400 transition-colors">Acerca de mi</a>
                <a href="#habilidades" class="text-gray-700 dark:text-gray-300 text-sm font-medium leading-normal hover:text-blue-600 dark:hover:text-blue-400 transition-colors">Habilidades</a>
                <a href="#projects" class="text-gray-700 dark:text-gray-300 text-sm font-medium leading-normal hover:text-blue-600 dark:hover:text-blue-400 transition-colors">Proyectos</a>
                <a href="#contact" class="text-gray-700 dark:text-gray-300 text-sm font-medium leading-normal hover:text-blue-600 dark:hover:text-blue-400 transition-colors">Contacto</a>
                <button id="theme-toggle" class="p-2 rounded-lg bg-gray-100 dark:bg-gray-800 hover:bg-gray-200 dark:hover:bg-gray-700 transition-colors" aria-label="Cambiar tema">
                    <svg id="theme-toggle-dark-icon" class="w-5 h-5 text-gray-700 dark:text-gray-300" fill="currentColor" viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg">
                        <path d="M17.293 13.293A8 8 0 016.707 2.707a8.001 8.001 0 1010.586 10.586z"></path>
                    </svg>
                    <svg id="theme-toggle-light-icon" class="w-5 h-5 text-gray-700 dark:text-gray-300 hidden" fill="currentColor" viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg">
                        <path d="M10 2a1 1 0 011 1v1a1 1 0 11-2 0V3a1 1 0 011-1zm4 8a4 4 0 11-8 0 4 4 0 018 0zm-.464 4.95l.707.707a1 1 0 001.414-1.414l-.707-.707a1 1 0 00-1.414 1.414zm2.12-10.607a1 1 0 010 1.414l-.706.707a1 1 0 11-1.414-1.414l.707-.707a1 1 0 011.414 0zM17 11a1 1 0 100-2h-1a1 1 0 100 2h1zm-7 4a1 1 0 011 1v1a1 1 0 11-2 0v-1a1 1 0 011-1zM5.05 6.464A1 1 0 106.465 5.05l-.708-.707a1 1 0 00-1.414 1.414l.707.707zm1.414 8.486l-.707.707a1 1 0 01-1.414-1.414l.707-.707a1 1 0 011.414 1.414zM4 11a1 1 0 100-2H3a1 1 0 000 2h1z" fill-rule="evenodd" clip-rule="evenodd"></path>
                    </svg>
                </button>
            </div>

            <!-- Mobile: Theme Toggle + Hamburger -->
            <div class="flex md:hidden items-center gap-2">
                <button id="theme-toggle-mobile" class="p-2 rounded-lg bg-gray-100 dark:bg-gray-800 hover:bg-gray-200 dark:hover:bg-gray-700 transition-colors" aria-label="Cambiar tema">
                    <svg id="theme-toggle-dark-icon-mobile" class="w-5 h-5 text-gray-700 dark:text-gray-300" fill="currentColor" viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg">
                        <path d="M17.293 13.293A8 8 0 016.707 2.707a8.001 8.001 0 1010.586 10.586z"></path>
                    </svg>
                    <svg id="theme-toggle-light-icon-mobile" class="w-5 h-5 text-gray-700 dark:text-gray-300 hidden" fill="currentColor" viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg">
                        <path d="M10 2a1 1 0 011 1v1a1 1 0 11-2 0V3a1 1 0 011-1zm4 8a4 4 0 11-8 0 4 4 0 018 0zm-.464 4.95l.707.707a1 1 0 001.414-1.414l-.707-.707a1 1 0 00-1.414 1.414zm2.12-10.607a1 1 0 010 1.414l-.706.707a1 1 0 11-1.414-1.414l.707-.707a1 1 0 011.414 0zM17 11a1 1 0 100-2h-1a1 1 0 100 2h1zm-7 4a1 1 0 011 1v1a1 1 0 11-2 0v-1a1 1 0 011-1zM5.05 6.464A1 1 0 106.465 5.05l-.708-.707a1 1 0 00-1.414 1.414l.707.707zm1.414 8.486l-.707.707a1 1 0 01-1.414-1.414l.707-.707a1 1 0 011.414 1.414zM4 11a1 1 0 100-2H3a1 1 0 000 2h1z" fill-rule="evenodd" clip-rule="evenodd"></path>
                    </svg>
                </button>
                <button id="mobile-menu-toggle" class="p-2 rounded-lg bg-gray-100 dark:bg-gray-800 hover:bg-gray-200 dark:hover:bg-gray-700 transition-colors" aria-label="Abrir menú">
                    <svg id="hamburger-icon" class="w-6 h-6 text-gray-700 dark:text-gray-300" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"></path>
                    </svg>
                    <svg id="close-icon" class="w-6 h-6 text-gray-700 dark:text-gray-300 hidden" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path>
                    </svg>
                </button>
            </div>
        </div>

        <!-- Mobile Menu (Posición absoluta para sobreponerse) -->
        <div id="mobile-menu" class="absolute left-0 right-0 top-full md:hidden bg-white dark:bg-[#111921] border-t border-gray-200 dark:border-gray-700 shadow-lg opacity-0 invisible transition-all duration-300 transform -translate-y-2">
            <div class="flex flex-col space-y-1 py-4 px-4 sm:px-6 lg:px-8">
                <a href="#home" class="mobile-menu-link text-gray-700 dark:text-gray-300 text-sm font-medium py-3 px-4 rounded-lg hover:bg-gray-100 dark:hover:bg-gray-800 transition-colors">Inicio</a>
                <a href="#about" class="mobile-menu-link text-gray-700 dark:text-gray-300 text-sm font-medium py-3 px-4 rounded-lg hover:bg-gray-100 dark:hover:bg-gray-800 transition-colors">Acerca de mi</a>
                <a href="#habilidades" class="mobile-menu-link text-gray-700 dark:text-gray-300 text-sm font-medium py-3 px-4 rounded-lg hover:bg-gray-100 dark:hover:bg-gray-800 transition-colors">Habilidades</a>
                <a href="#projects" class="mobile-menu-link text-gray-700 dark:text-gray-300 text-sm font-medium py-3 px-4 rounded-lg hover:bg-gray-100 dark:hover:bg-gray-800 transition-colors">Proyectos</a>
                <a href="#contact" class="mobile-menu-link text-gray-700 dark:text-gray-300 text-sm font-medium py-3 px-4 rounded-lg hover:bg-gray-100 dark:hover:bg-gray-800 transition-colors">Contacto</a>
            </div>
        </div>
    </div>
</nav>

<script>
    document.addEventListener('DOMContentLoaded', () => {
        const html = document.documentElement;
        
        // Desktop theme toggle
        const themeToggle = document.getElementById('theme-toggle');
        const darkIcon = document.getElementById('theme-toggle-dark-icon');
        const lightIcon = document.getElementById('theme-toggle-light-icon');
        
        // Mobile theme toggle
        const themeToggleMobile = document.getElementById('theme-toggle-mobile');
        const darkIconMobile = document.getElementById('theme-toggle-dark-icon-mobile');
        const lightIconMobile = document.getElementById('theme-toggle-light-icon-mobile');
        
        // Mobile menu
        const mobileMenuToggle = document.getElementById('mobile-menu-toggle');
        const mobileMenu = document.getElementById('mobile-menu');
        const hamburgerIcon = document.getElementById('hamburger-icon');
        const closeIcon = document.getElementById('close-icon');
        const mobileMenuLinks = document.querySelectorAll('.mobile-menu-link');

        // Función para actualizar iconos de tema
        function updateThemeIcons() {
            const isDark = html.classList.contains('dark');
            
            // Desktop icons
            if (isDark) {
                darkIcon?.classList.remove('hidden');
                lightIcon?.classList.add('hidden');
            } else {
                darkIcon?.classList.add('hidden');
                lightIcon?.classList.remove('hidden');
            }
            
            // Mobile icons
            if (isDark) {
                darkIconMobile?.classList.remove('hidden');
                lightIconMobile?.classList.add('hidden');
            } else {
                darkIconMobile?.classList.add('hidden');
                lightIconMobile?.classList.remove('hidden');
            }
        }

        // Función para toggle del tema
        function toggleTheme() {
            html.classList.toggle('dark');
            updateThemeIcons();
            localStorage.setItem('theme', html.classList.contains('dark') ? 'dark' : 'light');
        }

        // Función para toggle del menú móvil
        function toggleMobileMenu() {
            const isOpen = mobileMenu?.classList.contains('menu-open');
            
            if (isOpen) {
                // Cerrar con animación
                mobileMenu?.classList.remove('menu-open', 'opacity-100', 'visible', 'translate-y-0');
                mobileMenu?.classList.add('opacity-0', 'invisible', '-translate-y-2');
                hamburgerIcon?.classList.remove('hidden');
                closeIcon?.classList.add('hidden');
            } else {
                // Abrir con animación
                mobileMenu?.classList.add('menu-open', 'opacity-100', 'visible', 'translate-y-0');
                mobileMenu?.classList.remove('opacity-0', 'invisible', '-translate-y-2');
                hamburgerIcon?.classList.add('hidden');
                closeIcon?.classList.remove('hidden');
            }
        }

        // Inicializar iconos
        updateThemeIcons();

        // Event listeners para tema (desktop y mobile)
        themeToggle?.addEventListener('click', toggleTheme);
        themeToggleMobile?.addEventListener('click', toggleTheme);

        // Event listener para menú móvil
        mobileMenuToggle?.addEventListener('click', toggleMobileMenu);

        // Cerrar menú al hacer click en un link
        mobileMenuLinks.forEach(link => {
            link.addEventListener('click', () => {
                toggleMobileMenu();
            });
        });

        // Cerrar menú al hacer scroll
        let lastScroll = 0;
        window.addEventListener('scroll', () => {
            const currentScroll = window.pageYOffset;
            const isOpen = mobileMenu?.classList.contains('menu-open');
            if (Math.abs(currentScroll - lastScroll) > 50 && isOpen) {
                toggleMobileMenu();
            }
            lastScroll = currentScroll;
        });
    });
</script>
